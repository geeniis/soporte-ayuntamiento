{
  "name": "intranet-ayuntamiento",
  "private": true,
  "scripts": {
    "dev": "npm --prefix web run dev",
    "install:web": "npm --prefix web install",
    "build:web": "npm --prefix web run build",
    "start:web": "npm --prefix web run start"
  },
  "devDependencies": {
    "prisma": "^6.17.1"
  },
  "dependencies": {
    "@prisma/client": "^6.17.1"
  }
}
